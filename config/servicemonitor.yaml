apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: twinward
  namespace: twinward-system
  labels:
    app.kubernetes.io/name: twinward
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: twinward
  endpoints:
    - port: metrics
      path: /metrics
