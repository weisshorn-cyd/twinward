extends: default

ignore: |
  .agents/
  .codex/
  .git/
  .gocache/
  .gotmp/

rules:
  comments:
    min-spaces-from-content: 1
  document-start: disable
  line-length:
    max: 120
  truthy:
    allowed-values:
      - "true"
      - "false"
    check-keys: false
